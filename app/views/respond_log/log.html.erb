<!-- <h1>RespondLog#log</h1>
<p>Find me in app/views/respond_log/log.html.erb</p>
-->

  <h4> Журнал сбора статистики </h4> 
  <p><a href="/dj_mon">DJ_Mon(логин и пароль пустые)</a></p>
  <p><a href="/statistic/index">Просмотр статистики в текстовом виде</a></p>


<table border="1">
  <% @log.each do |l| %>
    <tr>
      <td> 
        <img src=<%= case l.status
                        when "success"
                            "assets/success.png"
                        when "fail"
                            "assets/fail.png"
                        when "error"
                            "assets/error.png"
                        else
                            0
                      end %>> 
      </td>
      <td> <%= l.created_at %> </td>
      <td> <%= l.region.name %> </td>
      <td> <%= l.status %> </td>
      <td> <%= l.message %> </td>
    </tr>
  <% end %>
</table>

